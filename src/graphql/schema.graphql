scalar JSON

type Query {
    hello: String!
}

type Mutation {
    quote(symbol: String!): QuoteResult!
}

type QuoteResult {
    change: Float!
    changePercent: Float!
    companyName: String!
    peRatio: Float
    delayedPrice: Float
    previousClose: Float!
    symbol: String!
}
